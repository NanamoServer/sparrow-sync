package net.momirealms.sparrow.sync.plugin.logger;

public enum LogCategory {
    JOIN,
    QUIT,
    KICK,
    APPLY,
    SAVE,
    RETRY,
    STASH,
    STORAGE,
    LOCK,
    DATA,
    LIFECYCLE
}
